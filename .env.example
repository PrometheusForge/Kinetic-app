# Required for real CRM forwarding
CRM_WEBHOOK_URL=https://your-crm-or-automation-webhook.example/lead

# Optional if your endpoint needs a header
CRM_AUTH_HEADER=Authorization
CRM_AUTH_TOKEN=Bearer your-token-here
